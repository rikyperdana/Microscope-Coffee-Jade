a.dropdown-toggle(href="#", data-toggle="dropdown") Notifications
	if notificationCount
		span.badge.badge-inverse {{notificationCount}}
	b.caret
	
ul.notification.dropdown-menu
	if notificationCount
		{{#each notifications}} {{> notificationItem}} {{/each}}
	else
		li
			span No notifications
		

//	{{#if notificationCount}} <span class="badge badge-inverse">{{notificationCount}}</span> {{/if}}
//	{{#if notificationCount}} {{#each notifications}} {{> notificationItem}} {{/each}} {{else}} <li><span>No notifications</span></li> {{/if}}

